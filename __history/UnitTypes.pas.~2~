unit UnitTypes;

interface

const
   NameLength = 10;

type
   TIndex = Word;
   TAmount = TIndex;
   TEdgeValue = Integer;
   TName = String[NameLength];
   TDirection = (DBidirectional, DUnidirectional);

implementation

end.
